---
  import { classNaming }  from "$scripts/_className"
let blk: HeadingBlock<IsSingle>
({ blk } = Astro.props)

const presets = {
  def: "title is-4 my-4",
  center: "title is-4 my-4 has-text-centered",
};

const cls = classNaming("", blk.classes, blk.preset, presets)
---

<h4 id={blk.id} class={cls}>
  {blk.$text}
</h4>
