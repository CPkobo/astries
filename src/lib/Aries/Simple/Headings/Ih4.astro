---
  import { classNaming }  from "$scripts/_className"
let blk: IconHeadingBlock<string>
({ blk } = Astro.props)

const presets = {
  def: "title is-2 has-text-primary mt-2",
};

const cls = classNaming("", blk.classes, blk.preset, presets)
---

<h4 id={blk.id} class={cls}>
  {
    blk.icon !== ""
      ? <span className="icon mx-3">
          <i className={blk.icon} aria-hidden="true" />
        </span>
      : ""
  }
  {blk.$text}
</h4>

<style lang="scss">
  h4 {
    border-bottom: solid 2px teal;
  }
</style>
